import React from 'react';
import Chip from '@mui/material/Chip';

const FoundChip = () => {
  return <Chip size="small" variant="outlined" color="error" label="Found!" />;
};

export default FoundChip;
